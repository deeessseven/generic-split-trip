{
  "name": "generic-split-trip",
  "version": "1.0.0",
  "description": "Dual-view obstacle dodger — top-down + side view split screen",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:variants": "node scripts/build-variants.mjs",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "vite": "^5.4.0"
  }
}
