{
  "name": "generic-split-trip",
  "version": "1.1.16",
  "description": "Dual-view obstacle dodger — top-down + side view split screen",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:variants": "node scripts/build-variants.mjs",
    "build:app": "node scripts/build-app.mjs",
    "test": "node scripts/test-relayout.mjs",
    "preview": "vite preview",
    "cap:sync": "npm run build:app && npx cap sync",
    "cap:android": "npm run build:app && npx cap sync && npx cap open android",
    "cap:ios": "npm run build:app && npx cap sync && npx cap open ios",
    "assets": "capacitor-assets generate --android && node scripts/fix-adaptive-icon.mjs"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "vite": "^5.4.0"
  }
}
