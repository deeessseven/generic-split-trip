# gametext.txt — Generic Split Trip
# Edit the values below to customize your variant.
# Lines starting with # are comments and are ignored.
# Use \n for a line break inside a value.

# ── Identity ──────────────────────────────────────────────────────────────────
gameTitle       = Split Trip
gameSubtitle    = Control two dimensions — survive both views
copyright       = © 2026 Split Trip

# ── Hero / story (leave blank if unused) ──────────────────────────────────────
heroName        =
heroDescription =
storyLine1      =
storyLine2      =
storyLine3      =

# ── Menu tips ─────────────────────────────────────────────────────────────────
tipLeftLabel    = LEFT PANEL
tipLeftDesc     = ←→ Drag finger left/right to steer (top-down view)
tipRightLabel   = RIGHT PANEL
tipRightDesc    = Tap to fly up — release and you fall (side view)
tipSurviveLabel = SURVIVE
tipSurviveDesc  = Go through the gaps in both panels

# ── In-game labels ────────────────────────────────────────────────────────────
labelTopView    = TOP VIEW
labelTopHint    = ← drag →
labelSideView   = SIDE VIEW
labelSideHint   = tap to rise
hintSteer       = ↔ Drag to steer
hintRise        = Tap to rise ↑

# ── Score ─────────────────────────────────────────────────────────────────────
scoreUnit       = walls
scoreBest       = Best
scoreSurvived   = s survived

# ── Game Over ─────────────────────────────────────────────────────────────────
gameOverTitle   = GAME OVER
btnPlayAgain    = PLAY AGAIN
btnMainMenu     = MAIN MENU

# ── Settings ──────────────────────────────────────────────────────────────────
settingsTitle    = CUSTOMIZE SPRITES
settingsSubtitle = Tap to upload a custom image — tap [reset] to restore default
slotCharTop      = Character\nTop View
slotCharSide     = Character\nSide View
slotBgTop        = Background\nTop View
slotBgSide       = Background\nSide View
slotObstacle     = Obstacle\nTexture
slotHitMark      = Collision\nMark
btnBack          = BACK
toastSpriteSaved = Sprite saved! It will apply next game.
toastUploadError = Could not read that image — try another file.

# ── adrianas-split-trip: birthday celebration text ──────────────────────────────
# This file is APPENDED onto the base gametext.txt at build time (npm run build:variants),
# so it only needs the celebration keys — everything else is inherited from the base game.
# Edit freely. Use \n for a line break. Lines starting with # are comments.
#
# Tokens you can use in the View 2 (milestone) lines:
#   {N}       -> the number below (celebN), e.g. 13
#   {Nth}     -> the ordinal below (celebNth), e.g. 13th
#   {walls}   -> walls passed in that run (live)
#   {seconds} -> seconds survived in that run (live)

# ── Variant identity (the base game is now generic, so re-assert Adriana's branding here) ──
gameTitle     = Adriana's Birthday\nAdventure
gameOverTitle = GAME OVER, but\nHAPPY BDAY SMALL DINO!

# ── View 1: celebration shown right after tapping PLAY (before the game starts) ──
celebTitle    = HAPPY BIRTHDAY\nSMALL DINO!
celebMsg1     = Adriana, today is all about YOU.
celebMsg2     = Wishing my favorite Small Dino the silliest, happiest, most roar-some birthday ever!
celebMsg3     = Love always, your Funcle David
celebTapStart = Tap anywhere to START THE GAME

# ── View 2: celebration after a crash, ONLY when you pass {N} or more walls ──
# (celebN is both the displayed number AND the threshold. To change the birthday, edit BOTH
#  celebN and celebNth — ordinals like 13th/21st are irregular, so they're set by hand.)
celebN               = 13
celebNth             = 13th
milestoneTitle       = HAPPY {Nth} BIRTHDAY\nSMALL DINO!
milestoneMsg1        = You surpassed {walls} walls and survived {seconds}s — incredible!
milestoneMsg2        = {Nth} birthday AND achieved a {N}-wall milestone. Funcle David is SO proud of you!
milestoneTapContinue = Tap anywhere to continue
