# gametext.txt — JD's Split Trip (COMPLETE variant gametext)
# A full copy of the base Split Trip gametext with JD's overrides applied inline,
# plus the celebration keys at the bottom. Edit ANY value below to customize JD's game.
# At build time this REPLACES the base gametext for the jd variant.
# Lines starting with # are comments. Use \n for a line break inside a value.

# ── Identity ──────────────────────────────────────────────────────────────────
gameTitle       = Jackie + David's\nCrash Landing
gameSubtitle    = Control two dimensions — survive both views
copyright       = © 2026 Split Trip

# ── Hero / story (leave blank if unused) ──────────────────────────────────────
heroName        =
heroDescription =
storyLine1      =
storyLine2      =
storyLine3      =

# ── Menu tips ─────────────────────────────────────────────────────────────────
tipLeftLabel    = LEFT PANEL
tipLeftDesc     = ←→ Drag finger left/right to steer (top-down view)
tipRightLabel   = RIGHT PANEL
tipRightDesc    = Tap to fly up — release and you fall (side view)
tipSurviveLabel = SURVIVE
tipSurviveDesc  = Go through the gaps in both panels

# ── In-game labels ────────────────────────────────────────────────────────────
labelTopView    = TOP VIEW
labelTopHint    = ← drag →
labelSideView   = SIDE VIEW
labelSideHint   = tap to rise
hintSteer       = ↔ Drag to steer
hintRise        = Tap to rise ↑

# ── Score ─────────────────────────────────────────────────────────────────────
scoreUnit       = walls
scoreBest       = Best
scoreSurvived   = s survived

# ── Game Over ─────────────────────────────────────────────────────────────────
gameOverTitle   = GAME OVER
btnPlayAgain    = PLAY AGAIN
btnMainMenu     = MAIN MENU

# ── Settings ──────────────────────────────────────────────────────────────────
settingsTitle    = CUSTOMIZE SPRITES
settingsSubtitle = Tap to upload a custom image — tap [reset] to restore default
slotCharTop      = Character\nTop View
slotCharSide     = Character\nSide View
slotBgTop        = Background\nTop View
slotBgSide       = Background\nSide View
slotObstacle     = Obstacle\nTexture
slotHitMark      = Collision\nMark
btnBack          = BACK
toastSpriteSaved = Sprite saved! It will apply next game.
toastUploadError = Could not read that image — try another file.

# ═════════════════════════════════════════════════════════════════════════════
# jd — celebration scenes (View 1 + View 2)
# Everything ABOVE is the full base gametext (edit freely). Below are the celebration keys.
# Tokens for the milestone (View 2) lines:
#   {N} -> celebN number   {Nth} -> celebNth ordinal   {walls} -> walls passed   {seconds} -> seconds survived
# ═════════════════════════════════════════════════════════════════════════════

# ── View 1: celebration shown right after tapping PLAY (before the game starts) ──
celebTitle    = HAPPY 12 MONTHS!\nMISS BRUIN BEAR!
celebMsg1     = Jackie, I cannot believe we met 1 year ago.\nTime really flies when we're having fun.
celebMsg2     = It's been so wonderful having you in my life.\nI feel so lucky to be with you.
celebMsg3     = I can't wait to spend countless more years with you!\nI LOVE YOU!\n─David
celebTapStart = Tap anywhere to START THE GAME

# ── View 2: celebration after a crash, ONLY when you pass {N} or more walls ──
# (celebN is both the displayed number AND the threshold. To change the celebration day, edit BOTH
#  celebN and celebNth — ordinals like 13th/21st are irregular, so they're set by hand.)
celebN               = 12
celebNth             = 12th
milestoneTitle       = HAPPY {Nth} MONTH\nPrincess (of Pflugerville)!
milestoneMsg1        = {N} months AND at least a {N}-wall milestone.
milestoneMsg2        = You passed {walls} walls and survived {seconds}s — Incredible!\nI am SO proud of you and of us!\nKI SUH NUN!\nLove, Your David
milestoneTapContinue = Tap anywhere to continue

# ── Debug (temporary) ──
debugOutline = false
